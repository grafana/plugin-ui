{
    "$schema": "../schema.json",
    "schemaVersion": "v1",
    "pluginType": "grafana-netlify-datasource",
    "pluginName": "Netlify",
    "docURL": "https://grafana.com/docs/plugins/grafana-netlify-datasource",
    "fields": [
        {
            "id": "secureJsonData.Netlify.token",
            "key": "Netlify.token",
            "label": "Token",
            "description": "Netlify REST API Key. Found at https://app.netlify.com/user/applications#personal-access-tokens.",
            "valueType": "string",
            "target": "secureJsonData",
            "semanticType": "token",
            "required": true,
            "ui": {
                "placeholder": "Token value"
            }
        }
    ],
    "groups": [
        {
            "id": "auth",
            "title": "Authentication",
            "description": "Personal access tokens — Netlify REST API Key.",
            "fieldRefs": [
                "secureJsonData.Netlify.token"
            ]
        }
    ]
}
